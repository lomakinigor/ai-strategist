# Сессия 2026-04-27

## Тема сессии

Корректировка продуктовой логики ai-strategist. F-000A / T-000A.

## Что изменили

**CLAUDE.md**
- Обновлена идентичность проекта: 5 выходов вместо 4 (добавлено исследование аудитории)
- Добавлен явный UX-принцип: «Минимальное количество вопросов, максимум автоматического исследования»
- Обновлён продуктовый скоуп MVP: минимальный intake → auto-research pipeline

**docs/PRD.md**
- Переписан раздел «Решение»: явный 5-шаговый flow
- Зафиксировано: MVP — это автоматизированный workflow, а не чат-интервью
- Добавлен первичный пользовательский flow (intake → auto-research → validation → generation → report)
- Обновлены критерии успеха и MVP scope

**docs/features.md**
- F-003: переименован в «Minimal Research Intake», добавлен UX-принцип минимума вопросов
- F-004: переименован в «Auto-Research Pipeline», явно описаны 4 потока
- F-007: обновлён под 5 разделов + fallback-логика
- F-008: обновлены разделы отчёта (добавлена аудитория)

**docs/plan.md**
- Этап 2 → «Minimal Intake»
- Этап 3 → «Auto-Research Pipeline» с явными 4 потоками
- Добавлен Этап 4 (Validation) как отдельный шаг
- Этапы 4–5 → 5–6 (сдвиг нумерации)

**docs/tasks.md**
- Добавлен T-000A (Done)
- T-004: минимальная форма, явный запуск pipeline после submit
- T-005: 4 mock-адаптера с research_type
- T-008: 5 разделов, fallback-уточнение как исключение
- T-009: 5 разделов в workspace

**docs/tech-spec.md**
- Добавлена схема auto-research pipeline
- Обновлена App Router структура (добавлен /research/[id])
- Добавлен `/api/research/start` и `/api/research/status/[id]`
- `ResearchType` добавлен в типы

**docs/data-model.md**
- Добавлена таблица `research_jobs` (tracking 4 потоков)
- `facts.research_type` — 4 значения: business/market/audience/channels
- `facts.category` заменён на `facts.research_type`
- Обновлены связи: facts → research_jobs

**docs/user-stories.md**
- Добавлены: US-010 (нет уточнений), US-011 (прогресс), US-012 (аудитория), US-013 (автоматизация), US-014 (доверие к данным)
- Обновлены: US-001 (короткая форма), US-005 (5 разделов)

**MEMORY.md**
- Обновлён статус проекта
- Добавлены 4 новых решения

## Зафиксированные решения

- **Минимальный intake** — 5–6 полей, остальное система собирает сама
- **Auto-research** — 4 параллельных потока: business / market / audience / channels
- **Исследование аудитории** — обязательный этап, не опциональный
- **research_jobs** — отдельная таблица для tracking статуса каждого потока
- **Fallback-вопросы** — только при критической нехватке данных, не в обычном flow
- **Формат отчёта** — 5 разделов (бизнес/рынок/аудитория/каналы/стратегия), детали уточняются после MVP

## TODO (следующие шаги)

- [ ] T-001: Настройка Next.js 14 + TypeScript + Tailwind v4 + shadcn/ui
- [ ] T-002: Схема БД (+ research_jobs, facts.research_type)
- [ ] T-003: Reliability Engine

---

## Итерация 2 (F-000A / T-000A — второй проход)

### Что изменили

**CLAUDE.md** — добавлен раздел «Принцип клиентского отчёта»: отчёт = главный результат; MVP-формат рабочий и временный; финальный формат — после первых тестов

**docs/PRD.md** — добавлен раздел «После MVP — доработка формата клиентского отчёта»

**docs/features.md** — добавлен F-010 (Report Output Refinement, Future); в F-008 добавлен note о временном формате; F-009 описан как изолированный модуль

**docs/plan.md** — добавлен Этап 7 (post-MVP Report Output Refinement)

**docs/tasks.md** — T-000A расширен с полными критериями; добавлен T-010 (Future)

**docs/tech-spec.md** — добавлен раздел «Report / Export Layer» как изолированный модуль; архитектурное правило о конфигурируемых шаблонах

**docs/user-stories.md** — добавлен US-015 (команда продукта: тесты перед финализацией формата)

**MEMORY.md** — добавлены 3 новых решения

### Зафиксированные решения

- **Клиентский отчёт** — главный результат, не техническая деталь
- **MVP-формат** — рабочий и временный, не финальный
- **F-010 / T-010** — отдельный этап после первых реальных тестов
- **Report/export layer** — изолирован, можно переработать независимо

## Сессия 7 — F-003 / T-004 (Minimal Research Intake Form)

### Что создали

**`app/intake/page.tsx`** — Server Component, форма с 5 полями:
- Название компании (required)
- Отрасль / ниша (required)
- Краткое описание бизнеса (optional)
- Сайт компании (optional, type=url)
- Цель исследования (optional)

**`app/intake/actions.ts`** — Server Action `createResearchJob(formData)`:
1. Парсит FormData, валидирует name + industry
2. Создаёт запись `companies` (name, industry, description, website, goals, region='RU')
3. Создаёт запись `intake_submissions` (inputPayload — jsonb с raw формой)
4. Создаёт запись `research_jobs` (status='pending', все 4 потока = pending)
5. Redirect → `/research/${job.id}`

**`app/research/[id]/page.tsx`** — Страница статуса:
- Fetches researchJob + company по id
- Показывает название компании, отрасль, цель
- Таблица 4 потоков с цветными статусами (pending/running/done/error)
- Заглушка: реальное исследование в T-005+

**`app/page.tsx`** — Обновлён: добавлена кнопка «Создать исследование» → /intake

**`src/db/index.ts`** — Переведён с module-level throw на ленивый `getDb()`:
- Без этого `npm run build` падает при наличии server action, импортирующего db
- `_db` мемоизирован, client создаётся один раз при первом вызове

### Зафиксированные решения

- **Ленивый getDb()** — обязательно для server actions в Next.js; module-level throw ломает build
- **Server Action без useState** — форма работает как нативный HTML form, JS не обязателен для submit
- **Intake → research_job** — один research_job на компанию в MVP, все 4 потока = pending до T-005

### Проверки

- `npm run test`: 25/25 ✓ (тесты T-003 остались зелёными)
- `npm run build`: ✓ без ошибок
- TypeScript: ✓ (tsc встроен в build)

### Статус

T-004: Done ✓

---

## Статус

F-000A / T-000A — ЗАВЕРШЁН ✓ (2 итерации)

---

## Сессия 2 — F-000B / T-000B (Sound Notifications)

### Что настроено

- **Файл:** `C:/Users/Lenovo/.claude/settings.json`
- **Stop hook** → двойной бип 880 Hz (задача завершена)
- **Notification hook** → тройной бип 660 Hz (ожидание подтверждения)
- **Метод:** `[System.Console]::Beep()` через PowerShell — без внешних зависимостей
- **Проверка:** PowerShell-команда выполнена вручную — звук работает
- **Созданы:** knowledge/notifications.md, scripts/beep-done.ps1, scripts/beep-wait.ps1

### Ограничения

- `Console.Beep()` использует системный динамик — не работает если звук заглушён
- Хук `Notification` срабатывает на системные уведомления Claude Code, не на вопросы в чате
- При вопросах в чате (tool use confirmation) хук срабатывает через `Stop`-like поведение

---

## Итерация 2 — F-000B (Update Sound Notifications)

### Что изменили

- **`scripts/beep-done.ps1`** — `Console.Beep` → `System.Media.SoundPlayer` + синтез WAV в памяти. Два разных тона: 660 Hz (300 мс) → 880 Hz (400 мс). Громкость через аудиокарту (не системный динамик).
- **`scripts/beep-wait.ps1`** — то же. Три разных тона: 523 Hz → 659 Hz → 784 Hz (до-ми-соль, мажорное трезвучие).
- **`MEMORY.md`** — добавлены 2 незакоммиченных решения по DB schema (T-001/T-002).
- **`package-lock.json`** — добавлен в репозиторий (был untracked).
- **`C:/Users/Lenovo/.claude/settings.json`** — добавлен `PreToolUse` хук для Bash/Edit/Write/MultiEdit → тройной сигнал при диалоге подтверждения. Хуки Stop/Notification переведены на вызов `.ps1` файлов вместо inline-команды.

### Статус

- Коммит: `ac4309e` — `2026-04-27 F-000B: update sound notifications — distinct tones via SoundPlayer`
- Push: **не выполнялся** (локальный коммит)
- `settings.json` в коммит не входит (глобальный файл вне репозитория)

---

## Сессия 4 — F-001B / T-001B (Git Remote + Push + Deploy)

### Что сделано

- **Git remote origin** настроен: `https://github.com/lomakinigor/ai-strategist.git`
- **Push**: успешен — все 5 коммитов на GitHub (`master → origin/master`)
- **Deploy**: Vercel UI → Import Git Repository → `lomakinigor/ai-strategist` → Deploy
- **Production URL**: `https://ai-strategist-bice.vercel.app`
- **Deployment URL**: `https://ai-strategist-2v8ekzb5d-igorsibola.vercel.app`
- **Status**: Ready ✓, Source: `14f69b4` (master)
- **Smoke-check**: страница открывается, отображается заглушка AI-Стратег — success ✓
- **DATABASE_URL**: не задан — добавить в Vercel → Settings → Environment Variables перед production-использованием БД

### Коммиты в рамках F-001B

- `7030566` — `2026-04-27 F-001B/T-001B: configure git remote, push and deploy to Vercel`
  - Файлы: `docs/tasks.md` (добавлен T-001B), `memory/2026-04-27.md`
- `14f69b4` — `2026-04-27 F-001B: update memory with push/deploy status`

### Статус T-001B

**Done** ✓ — remote настроен, push выполнен, деплой через Vercel UI успешен, smoke-check пройден

---

## Сессия 6 — F-000C / T-000C (Fix Notification Hooks + Mandatory Summary)

### Что изменили

- **`~/.claude/settings.json`** — удалены все PreToolUse hooks (Bash/Edit/Write/MultiEdit); остались только Stop и Notification
- **`scripts/beep-wait.ps1`** — паттерн изменён с восходящего (523→659→784) на нисходящий (784→659→523 Hz)
- **`CLAUDE.md`** — добавлен раздел «Обязательное резюме»: задача считается завершённой только при наличии блока «РЕЗЮМЕ ДЛЯ ПЕРЕДАЧИ В ЧАТ»; формат резюме зафиксирован
- **`knowledge/notifications.md`** — полностью актуализирован: таблица событий, объяснение удаления PreToolUse, ограничение по PermissionRequest
- **`docs/tasks.md`** — добавлен T-000C (Done)
- **`MEMORY.md`** — добавлены 3 решения

### Зафиксированные решения

- PreToolUse не является эквивалентом permission dialog — удалён
- Нет хука PermissionRequest в Claude Code: Notification — ближайший доступный аналог
- Тройной нисходящий сигнал: 784 → 659 → 523 Hz (соль-ми-до)
- Правило резюме жёсткое: нет блока = задача не завершена

### Статус

T-000C: Done ✓

---

## Сессия 5 — F-002 / T-003 (Reliability Engine + AI Provider Foundation)

### Что создали

**Reliability Engine (`src/lib/reliability/`):**
- `rules.ts` — `rsToConfidence()` + `classifyFactType()` с явными правилами
- `classify.ts` — `classify(rawPoint: RawDataPoint): VerifiedFact`
- `index.ts` — re-export
- `__tests__/classify.test.ts` — 17 unit-тестов

**AI Provider Abstraction (`src/lib/ai/`):**
- `types.ts` — AIProviderId, ResearchProvider, ResearchRequest, ResearchResult, ProviderCapabilities
- `config.ts` — AI_CONFIG: defaultProvider=perplexity, model=sonar-pro, fallback=mock
- `router.ts` — getResearchProvider(), getDefaultResearchProviderId()
- `providers/perplexity-research-provider.ts` — placeholder (TODO: PERPLEXITY_API_KEY)
- `providers/mock-research-provider.ts` — используется в тестах и как fallback
- `providers/index.ts`, `providers/research-provider.ts`
- `__tests__/router.test.ts` — 8 unit-тестов

**Прочее:**
- `vitest.config.ts` — минимальный конфиг
- `package.json` — добавлен vitest, scripts test/test:watch

### Зафиксированные решения

- RS 4–5 → HIGH, RS 3 → MEDIUM, RS 1–2 → LOW
- FACT = RS ≥ 3 + source + date; HYPOTHESIS = RS 2 или нет даты; INSUFFICIENT_DATA = RS ≤ 1 / нет source / нет data
- Perplexity Sonar (sonar-pro) = default research provider; реальные вызовы — в T-005
- Бизнес-логика вызывает провайдера только через router, не напрямую

### Статус

- Тесты: 25/25 ✓
- tsc --noEmit ✓
- T-003: Done ✓

---

## Сессия 3 — F-001 / T-001 + T-002 (Core Infrastructure + DB Schema)

### Что создали

**Конфиги:**
- `package.json` — Next.js 14.2.21, TypeScript, Tailwind v4, Drizzle ORM, Zod, Vercel AI SDK, shadcn/ui-ready
- `tsconfig.json` — strict mode, paths alias `@/*`
- `next.config.ts` — минимальный
- `postcss.config.mjs` — Tailwind v4 (@tailwindcss/postcss)
- `drizzle.config.ts` — dialect postgresql, schema `./src/db/schema.ts`
- `components.json` — shadcn/ui config
- `.gitignore`, `.env.example`

**Источники:**
- `src/db/schema.ts` — 9 таблиц, 7 pgEnum, полные relations
- `src/db/index.ts` — Drizzle client (postgres npm package)
- `src/lib/types.ts` — TypeScript-типы домена
- `src/lib/utils.ts` — cn() helper (clsx + tailwind-merge)

**App:**
- `app/layout.tsx`, `app/globals.css`, `app/page.tsx` — минимальный каркас

**Обновлены:**
- `docs/tasks.md` — T-001 и T-002 → Done
- `docs/data-model.md` — синхронизирован с реальной схемой

### Схема БД (9 таблиц)

users → companies → intake_submissions, research_jobs, sources, facts, strategy_artifacts, report_artifacts, embeddings

Ключевые решения:
- `facts.research_type` покрывает все 4 потока (business/market/audience/channels)
- `research_jobs` отслеживает статус каждого потока отдельно
- `report_artifacts.content_json` без жёсткой схемы (F-010 уточнит)
- pgvector: структура готова, vector-колонка добавляется в T-007

### Git / Deploy

- **Коммиты:**
  - `c3e1c1b` — init core infrastructure and database schema
  - `9faa4a1` — fix: next.config.ts → next.config.mjs (Next.js 14 не поддерживает .ts-конфиг)
- **Branch:** master
- **npm install:** успешно (421 пакет)
- **tsc --noEmit:** ✓ без ошибок
- **npm run build:** ✓ успешно
- **Push:** не выполнен — удалённый репозиторий не настроен
- **Deploy:** не выполнен — vercel CLI не установлен
- **Что нужно для деплоя:**
  1. `git remote add origin <github-url>` + `git push -u origin master`
  2. Подключить репозиторий в Vercel UI
  3. Добавить `DATABASE_URL` в Vercel env vars

---

## Сессия 8 — F-004 / T-005 (Auto-Research Mock Adapters + Lifecycle)

### Что создали

**Mock adapters (`src/lib/research/`):**
- `business-adapter.mock.ts` — 3 RawDataPoint: intake данные (RS:3), сайт (RS:4/2), placeholder (RS:2)
- `market-adapter.mock.ts` — 3 RawDataPoint: рынок (RS:3), конкуренция (RS:3), тренды (RS:2)
- `audience-adapter.mock.ts` — 3 RawDataPoint: сегменты (RS:2), потребности (RS:2), боли (RS:2)
- `channels-adapter.mock.ts` — 3 RawDataPoint: каналы (RS:3/2), рекомендации (RS:2), конкуренты (RS:2)
- `orchestrator.ts` — `startResearchJob(jobId)`: pending→running (Promise.all 4 адаптера)→classify()→insert facts→done
- `index.ts` — re-exports

**App:**
- `app/research/[id]/actions.ts` — Server Action `triggerMockResearch(formData)` вызывает orchestrator
- `app/research/[id]/page.tsx` — обновлён: кнопка запуска (pending), статус (running), mock-результаты + первый факт per stream (done)

**Tests:**
- `src/lib/research/__tests__/adapters.test.ts` — 18 тестов: структура, типы, RS-логика, classify() интеграция для всех 4 адаптеров

### Lifecycle research_job

```
pending → running (все 4 stream статуса = running) → [Promise.all адаптеры] → insert facts → done (все 4 stream = done)
```

### Архитектурные решения

- Orchestrator сам читает company из DB по job.companyId — сервис самодостаточен
- Факты вставляются без source record (sourceId nullable)
- Form + Server Action (не client-side JS) для кнопки запуска

### Тесты

- `npm run test`: **43/43** ✓ (было 25, добавилось 18)
- `npm run build`: ✓

### Git

- Commit: _(после выполнения)_
- Push: _(после выполнения)_
- Deploy: not required now

